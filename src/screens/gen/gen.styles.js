import {makeStyles} from "@material-ui/core/styles";

const useStyles = makeStyles({
    depositContext: {
        flex: 1,
        fontSize: 11,
        padding: '8px 0'
    },
});

export default useStyles
